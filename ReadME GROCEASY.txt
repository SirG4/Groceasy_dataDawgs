1. Install all required libraries
	pip install Flask
	pip install Flask-CORS
	pip install beautifulsoup4
	pip install requests
	pip install pandas
	pip install google-generativeai
2. Run app.py for the backend to start
	python app.py
3. Open index.html in a web browser to open the website
4. Enjoy